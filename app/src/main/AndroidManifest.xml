<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="me.koterwong.androidlibs">

  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
  <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

  <application
      android:name=".base.App"
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:supportsRtl="true"
      android:theme="@style/AppTheme">

    <!-- ==============================umeng Start==================================== -->
    <meta-data
        android:name="UMENG_APPKEY"
        android:value="57e4d8ed67e58e7ec7003081"/>
    <meta-data
        android:name="UMENG_CHANNEL"
        android:value="${GRADLE_CHANNEL_VALUE}"/>
    <!-- ==============================umeng end ===================================== -->

    <activity android:name=".ui.activity.MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
    <activity android:name=".ui.activity.LVActivity"/>
    <activity android:name=".ui.activity.RvActivity"/>
    <activity android:name=".ui.activity.SlideActivity"/>
    <activity android:name=".ui.activity.MaterialProgressActivity"/>
    <activity android:name=".ui.activity.SpotsDialogActivity"/>
    <activity android:name=".ui.activity.BottomDialogActivity"/>
    <activity android:name=".ui.activity.DataEpActivity"/>
  </application>

</manifest>